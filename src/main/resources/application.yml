spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: 3h05221021@gmail.com
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver


    jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  security:
    user:
      name: abcd
      roles: USER, ADMIN